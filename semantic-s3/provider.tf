#taking out provider and region input from code. user will have to 
#provide those info at run time. We are doing this to avoid hard coding 
#data and also create persistent backends for our remote backend

#provider "aws" {
#  region = var.region #required region

#}